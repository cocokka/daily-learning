# Getting Started

这是一个激活服务，是从别的项目clone过来并通过AI改造成Spring Boot + JDK25 + Maven的项目。
使用步骤：
1. clone项目`git clone`
2. 构建打包：`mvn clean package`
3. 部署运行：`java -jar target/jrebel-license-server-0.0.1-SNAPSHOT.jar`